name: Auto Calculate AHP TOPSIS SPKLU

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # penting supaya bisa pull/rebase

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install pandas numpy requests

      - name: Run AHP TOPSIS Analysis
        run: python main.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

          git add data_spklu.json ahp_results.json
          git commit -m "Auto update SPKLU data" || echo "No changes to commit"

          # ambil update terbaru dari repo (hindari conflict)
          git pull origin main --rebase

          # push kembali
          git push origin main
